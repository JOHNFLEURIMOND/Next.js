export declare module ClientOptions {
    class Configuration {
        constructor(useHttps: boolean, requestHost: string, timeout: number);
        useHttps: boolean;
        requestHost: string;
        timeout: number;
    }
    enum HttpMethod {
        GET = "GET",
        POST = "POST",
        DELETE = "DELETE",
        PUT = "PUT",
        OPTIONS = "OPTIONS",
        HEAD = "HEAD"
    }
    enum DefaultHeaderNames {
        SERVER_TOKEN = "X-Postmark-Server-Token",
        ACCOUNT_TOKEN = "X-Postmark-Account-Token"
    }
}
